{
  "_note": "Each query entry is a string or {query, maxPages} object. recentDays auto-sets the date window on every run. Edit queries here to change what gets monitored.",

  "queries": [
    {
      "_label": "Canary - broad coffee patents (health check)",
      "query": "AB=(coffee AND machine)",
      "maxPages": 10
    }
  ],

  "recentDays":   7,
  "maxPages":     2,

  "afterDate":    "",
  "beforeDate":   "",
  "type":         "PATENT",
  "status":       "",

  "fetchDelayMs": 1500,
  "pageDelayMs":  3000
}
